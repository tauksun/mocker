const backendBaseURL = "http://localhost:1234";
const createApiURL = backendBaseURL + "/api";
const getApiDataURL = backendBaseURL + "/api/data";
const storeApiDataURL = backendBaseURL + "/api/data";
const executeURL = backendBaseURL + "/testExecute";
const prettifyURL = backendBaseURL + "/api/prettify";

export {
  backendBaseURL,
  createApiURL,
  getApiDataURL,
  storeApiDataURL,
  executeURL,
  prettifyURL,
};
